<!DOCTYPE html>
<html>
  <head>
    <title><%= @show.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background-color: #000;
        color: #fff;
      }

      .banner {
        max-height: 500px;
        object-fit: cover;
        width: 100%;
        border-radius: 10px;
      }

      .episode, .actor {
        background-color: #111;
        border-radius: 10px;
        padding: 10px;
        margin-bottom: 10px;
      }

      .actor img {
        height: 100px;
        width: 100px;
        object-fit: cover;
        border-radius: 50%;
        margin-right: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container mt-4">
      <a href="/" class="btn btn-danger mb-4">← Voltar</a>

      <h1><%= @show.name %></h1>
      <img src="<%= @show.image %>" alt="<%= @show.name %>" class="banner mb-4">

      <p><%= @show.summary&.html_safe %></p>

      <h2 class="text-white text-xl font-bold mb-2">Episódios</h2>
      <ul class="list-unstyled">
        <% @episodes.each do |episode| %>
          <li class="episode">
            <%= link_to episode.name, episode_path(episode), class: "text-danger fw-bold" %>
          </li>
        <% end %>
      </ul>

      <h3 class="mt-5 text-white text-xl font-bold mb-2">Elenco</h3>
      <div class="row">
        <% @actors.each do |actor| %>
          <div class="col-md-4 actor d-flex align-items-center">
            <% if actor.image.present? %>
              <img src="<%= actor.image %>" alt="<%= actor.name %>">
            <% end %>
            <strong class="text-white"><%= actor.name %></strong>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
