<!DOCTYPE html>
<html>
  <head>
    <title>Netflix Clone</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background-color: #000;
        color: #fff;
      }
      .card {
        background-color: #111;
        border: none;
      }
      .card img {
        height: 300px;
        object-fit: cover;
      }
      a {
        text-decoration: none;
        color: #f00;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="mb-4">Séries</h1>
      <div class="row">
        <% @shows.each do |show| %>
          <div class="col-md-3 mb-4">
            <div class="card">
              <%= link_to show_path(show["id"]) do %>
                <img src="<%= show["image"] && show["image"]["medium"] %>" class="card-img-top" alt="<%= show["name"] %>">
                <div class="card-body">
                  <h5 class="card-title"><%= show["name"] %></h5>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
