<!DOCTYPE html>
<html>
  <head>
    <title>Séries</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background-color: #000;
        color: #fff;
      }

      .banner {
        max-height: 300px;
        object-fit: cover;
        width: 100%;
        border-radius: 10px;
        margin-bottom: 10px;
      }

      .serie {
        background-color: #111;
        border-radius: 10px;
        padding: 10px;
        margin-bottom: 10px;
      }

      .form-control::placeholder {
        color: #999;
      }
    </style>
  </head>
  <body>
    <div class="container mt-4">
      <h1 class="mb-3">Séries</h1>

      <!-- Campo de busca -->
      <%= form_with url: root_path, method: :get, local: true do %>
        <div class="input-group mb-4 w-25">
          <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Buscar série..." %>
          <button class="btn btn-danger" type="submit">Buscar</button>
        </div>
      <% end %>

      <!-- Resultados -->
      <div class="row">
        <% @shows.each do |show| %>
          <div class="col-md-4 serie">
            <%= link_to show.name, show_path(show.id), class: "text-white text-decoration-none" %>
            <% if show.image && show.image["medium"] %>
              <img src="<%= show.image["medium"] %>" alt="<%= show.name %>" class="banner">
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
