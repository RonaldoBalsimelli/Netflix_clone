<!DOCTYPE html>
<html>
<head>
  <title>Séries</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #000;
      color: #fff;
    }
    .banner {
      max-height: 300px;
      object-fit: cover;
      width: 100%;
      border-radius: 10px;
      margin-bottom: 10px;
    }
    .serie {
      background-color: #111;
      border-radius: 10px;
      padding: 10px;
      margin-bottom: 10px;
    }
    .form-control::placeholder {
      color: #999;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h1 class="mb-3">Séries</h1>

    <!-- Campo de busca -->
    <%= form_with url: root_path, method: :get, local: true do %>
      <div class="input-group mb-4 w-25">
        <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Buscar série..." %>
        <button class="btn btn-danger" type="submit">Buscar</button>
      </div>
    <% end %>

    <!-- Resultados -->
    <div class="row">
  <% @shows.each do |show| %>
    <div class="col-md-3 mb-4">
    <%= link_to show_path(show["id"]), class: "card d-block text-decoration-none text-white" do %>
      <% if show["image"] %>
        <img src="<%= show["image"]["medium"] %>" class="card-img-top" alt="<%= show["name"] %>">
      <% end %>
      <div class="card-body">
      <h5 class="card-title text-black"><%= show["name"] %></h5>

      </div>
    <% end %>    
    </div>
  <% end %>
</div>
  </div>
</body>
</html>
