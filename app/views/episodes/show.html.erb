<!DOCTYPE html>
<html>
<head>
  <title><%= @episode.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #000; color: #fff; }
    .container { margin-top: 40px; }
    img { border-radius: 10px; max-width: 100%; height: auto; }
  </style>
</head>
<body>
  <div class="container">
    <a href="<%= show_path(@show_id) %>" class="btn btn-danger mb-4">← Voltar para Série</a>
    <h1><%= @episode.name %></h1>
    <p><strong>Temporada:</strong> <%= @episode.season %> | <strong>Episódio:</strong> <%= @episode.number %></p>

    <% if @image %>
      <img src="<%= @image %>" alt="<%= @episode.name %>" class="mb-3">
    <% end %>

    <p><%= @translated_summary.html_safe %></p>
  </div>
</body>
</html>
