<!DOCTYPE html>
<html>
<head>
  <title><%= @episode.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #000;
      color: #fff;
    }
    .episode-img {
      max-width: 100%;
      border-radius: 10px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <a href="/" class="btn btn-danger mb-4">← Voltar</a>

    <h1><%= @episode.name %></h1>
    <p>Temporada <%= @episode.season %>, Episódio <%= @episode.number %></p>

    <% if @episode.image %>
      <img src="<%= @episode.image %>" alt="<%= @episode.name %>" class="episode-img">
    <% end %>

    <p><%= traduzir_para_portugues(@episode.summary)&.html_safe %></p>
  </div>
</body>
</html>
